# CLAUDE.md

このファイルは、本リポジトリで作業する Claude Code (claude.ai/code) へのガイダンスを提供します。

## プロジェクト概要

C++（CMake）の学習用アプリを47個作成するプロジェクト集。基礎文法からGUI・DB連携・通信・USBデバイス制御までを段階的に学ぶ。

- 各フォルダ（`01_BuildEnvironmentSetup` 〜 `47_USBFileTransferProtocol`）に独立した `CMakeLists.txt`／`CMakePresets.json`（`x64-debug`/`x64-release`）を配置し、課題ごとに個別ビルドする（リポジトリ全体を束ねるトップレベルのCMakeLists.txt・ソリューションファイルは存在しない）
- 番号帯でカテゴリを表現し、カテゴリごとに10番台ずつ空けている（後から課題を追加してもリナンバー不要）

  | 番号帯 | カテゴリ | 内容 |
  |--------|----------|------|
  | 01-08 | 基礎 (Fundamentals) | 環境構築、文法、メモリ管理、STL、OOP、例外、テンプレート |
  | 11-18 | GUIプログラミング | Win32 APIでのウィンドウ表示からマルチスレッドGUIまで |
  | 21-27 | DB連携 (Database) | SQLite/MySQL接続、CRUD、トランザクション、ORM |
  | 31-38 | 通信 (Networking) | TCP/UDP、HTTP、WebSocket、シリアル通信 |
  | 41-47 | USB | USBデバイス列挙、HID通信、ファイル転送 |

  詳細な課題一覧・学習の進め方は [README.md](README.md) を参照。

- **CMakeがビルド定義の唯一の正**。Visual Studioの `.sln`/`.vcxproj` は手動作成・コミットせず、「フォルダを開く」（Ninja、`out/build/x64-debug/`に出力）または `cmake -S . -B build -G "Visual Studio 18 2026" -A x64` でのその場生成（MSBuild、`build/`に出力）のいずれかで都度生成する。両方とも `.gitignore` 対象で、生成物は混在させない
- 開発環境にvcpkg/Conan等のパッケージマネージャが無く追加インストールもできないため、外部ライブラリは極力使わずWindows標準ライブラリ（Winsock2、WinHTTP、ODBC、CNG等）で構成する方針。やむを得ず外部ライブラリが必要な場合は `third_party/`（例: `third_party/sqlite3`）にソース一式をvendoringする。採用ライブラリの経緯・制約は [README.md](README.md) の該当セクションに詳しい

### プロジェクト構成の規約

- フォルダ名は `NN_AppName`（例: `01_BuildEnvironmentSetup`）だが、`CMakeLists.txt` の `project()`/`add_executable()` 名は数字プレフィックスなしの `AppName`（例: `BuildEnvironmentSetup`）
- 新規課題フォルダには `CMakeLists.txt` に加えて `CMakePresets.json`（[01_BuildEnvironmentSetup](01_BuildEnvironmentSetup/CMakePresets.json) をテンプレートとする）を必ず一緒に配置する
- ソースは `src/`、ヘッダーは `include/` に配置し、課題の狙い・学習ポイント・ビルド方法は各フォルダの `README.md` に記載する
- ビルド成果物（`build/`、`out/`）は `.gitignore` 対象。各課題フォルダ配下に生成されるが、コミットしない
- テストコードは本体と同じ `NN_AppName` フォルダ配下の `test` サブフォルダに配置する（構成の詳細は本ファイルの「テスト駆動開発」セクションを参照）
- 各アプリの実装は GitHub Issue 単位で管理する。実装前にプランを Issue コメントとして記録し、完了条件をチェックして PR 本文に `Closes #<Issue番号>` を記載する。Issue のスコープ外の追加実装（例: 学習ポイントにない機能追加）が発生した場合は別 Issue を起票し、元 Issue とは分けて追跡する

## よくある落とし穴

1. **日本語を扱う際の文字コード**: ソースはUTF-8で統一する。Windows(MSVC)では「コンパイル時に `CMakeLists.txt` で `/utf-8` フラグを付ける」「実行時に `main()`/`WinMain()` 先頭で `SetConsoleOutputCP(CP_UTF8)`（標準入力も扱うなら `SetConsoleCP(CP_UTF8)` も）を呼ぶ」の両方を対応しないと、日本語コメント以降が構文エラーになったり出力が文字化けしたりする（詳細・コード例は [README.md](README.md) 参照）
2. **無関係な既存バグを独断で処理してしまう**: 作業中に今のタスクと無関係な既存バグを見つけた場合、黙って直す・黙って無視するのではなく、見つけた時点でユーザーに扱い（今の PR に含めるか、別 PR に分けるか）を確認してから進めること
3. **改行コードの混入確認漏れ**: ファイルを編集した後は `git diff --stat` で差分行数を確認すること。編集ツールが意図せず LF/CRLF を変換すると、実際の変更が数行でもファイル全体が差分になってしまう。混入していたら想定の改行コードに戻してから再度確認する
4. **`windows.h` のグローバル関数・マクロと自作の識別子の衝突**: `windows.h` は `Rectangle`（GDI関数）や `max`/`min`（マクロ）等、汎用的な名前をグローバルに持ち込む。自作クラス名・変数名が衝突すると分かりにくいコンパイルエラーになる（[06_ClassesAndOOP](06_ClassesAndOOP/README.md) の `Rectangle` 衝突例を参照）。`NOGDI`/`NOMINMAX` 等のマクロで該当機能を除外するか、名前を変えて回避する
5. **ビルド前に対象アプリの起動中プロセスを確認する**: 実機確認のために起動した `.exe` を閉じずに `cmake --build` を実行すると、出力先の `.exe`/`.pdb` がロックされていてリンクに失敗する（「別のプロセスで使用されているためアクセスできません」等）。ビルドが原因不明で失敗する場合は同名プロセスが起動していないか確認する
6. **Win32 GUIの動作確認は実機操作 or `EnumWindows` ベースの自動化で行う**: ロジックの単体テストが通っていても、メッセージループ・コントロール配線のミスは検出できない。ビルド済み `.exe` を実際に操作してスクリーンショットを確認するか、`System.Windows.Automation` 等での外部自動化検証を行う。ただし本開発環境では外部スクリプトから `FindWindow`（クラス名・タイトルどちらでも）を呼ぶと対象ウィンドウが実在しても常に見つからない既知の問題があるため（[26_CustomerManagementApp](26_CustomerManagementApp) で確認済み）、`EnumWindows` で列挙し `GetWindowThreadProcessId` で対象プロセスIDと一致するものを探す方式を使うこと
7. **`.rc` リソース(`DIALOGEX`等)は既知の問題があるため小さく検証してから組み込む**: 本開発環境では `.rc` の `DIALOGEX` を `DialogBoxParam`/`CreateDialogParam` で読み込むと `GetLastError()=1814`（`ERROR_RESOURCE_NAME_NOT_FOUND`）で失敗する原因不明の問題がある（[14_DialogsAndMessageBox](14_DialogsAndMessageBox) で確認済み、回避策は `CreateWindowExW` ベースのポップアップウィンドウ実装）。アイコン・アクセラレータ・`DIALOGEX` 等 `.rc` リソースを新たに使う場合は、まず小さく検証してから本格的に組み込むこと

---

## 作業ルール

- **実装前にプランを提示すること**: コード変更を行う前に、方針・変更箇所・影響範囲を日本語でまとめたプランを提示し、ユーザーの承認を得てから実装に進む。
- **承認されたプランは `docs/{機能名}-Plan.md` として書き出す**（`docs/` は `.gitignore` 対象のローカル資料であり PR には含まれない）。実装の経緯を後から追えるようにする
- **複数項目をまとめて依頼された場合は 1 項目ずつサイクルを完結させること**: 「プラン提示 → 承認 → Issue作成/更新 → TDD 実装 → ビルド/テスト確認 → コミットメッセージ提示 → 承認 → コミット」のサイクルを 1 項目ごとに完了させてから次の項目に進む。TodoWrite で全項目の進捗を管理し、項目間で状態を見失わないようにする
- **軽微な修正は直接編集可**: 以下の軽微な修正はプラン提示なしで直接編集してよい。ただし変更内容が明らかな場合のみ:
  - スペルミス・タイポ修正
  - 単純な値の誤り（符号違い `+`/`-`、定数値の訂正など）
  - コメント・ドキュメントの文言修正（既存ドキュメントの改善）
  - IDE/コンパイラ警告削除（未使用 `#include`、未使用変数など）

  ただし、以下のような変更は軽微ではないため、プラン提示後に実装すること: ロジック変更が伴う修正、ファイル追加・削除、テスト追加、依存関係の変更
- **GitHub Flow に従うこと**: コード変更は必ず feature ブランチで行い、`main` に直接コミットしない。

  **ブランチ運用手順（GitHub Flow）**

  1. **ブランチ作成** — 作業開始前に `main` から Issue 単位でブランチを切る。ブランチ名は `issue-<Issue番号>-<内容を表す英語スラッグ>`とする（例: `issue-31-binary-protocol-design`）
     ```bash
     git switch main
     git pull
     git switch -c issue-<Issue番号>-<slug>
     ```
  2. **コミット** — 小さい単位でこまめにコミットする。コミットメッセージは変更の「なぜ」を説明する
  3. **プルリクエスト** — 実装が完了したら `main` への PR を作成する。PR タイトルは `<課題番号>: <概要>`（例: `36: マルチクライアントチャットサーバーを追加`、70 文字以内）とし、本文に概要・内容・動作確認・`Closes #<Issue番号>` を記載する。ラベルは紐づく Issue と同じカテゴリラベル（基礎/GUIプログラミング/DB連携/通信/USB）を設定する(`gh pr edit --add-label ...`)。本リポジトリはマイルストーンを使用していない
  4. **マージ後の後始末** — マージ後はローカル・リモートともにブランチを削除する
     ```bash
     git switch main && git pull
     git branch -d issue-<Issue番号>-<slug>
     ```

  **禁止事項**
  - `main` ブランチへの直接コミット（緊急の typo 修正など極めて軽微なものを除く）
  - `git push --force` を `main` ブランチに対して実行すること
  - **ユーザーの明示的な指示なしにコミットを実行すること** — 実装完了後はコミットメッセージ案を提示してユーザーの承認を得てからコミットする。「コミットしてください」「commit して」などの明示的な指示があった場合のみ実行する

  **コミットメッセージの書き方**
  - コミットメッセージは**日本語**で記述する
  - 型プレフィックス（`feat`/`fix`/`docs`/`refactor`/`test` など）は英語のままでよい
  - **メッセージは「何をした」よりも「なぜそうしたか」を説明する** — 変更の意図・理由・背景を含める
  - 例:
    - ✓ `fix: TCPサーバーで接続上限判定にTOCTOUがあった不具合を修正（チェックと登録の間に別スレッドが割り込める競合状態があったため）`
    - ✗ `fix: ChatServer::AddClient() を修正`（何をしたかだけで理由がない）
  - 例: `feat: 独自バイナリプロトコルにチェックサム検証を追加`、`docs: シリアル通信アプリのREADMEに配線図を追加`

- **テスト駆動開発を意識すること**: 新機能追加・バグ修正の際は、Red → Green → Refactor のサイクルを厳守する。

  **本リポジトリでのテスト方針**

  - 自動テストの対象は、GUI/ソケット通信/シリアル/USB等の実機・実環境に依存しない**純粋ロジック**（パーサー、統計計算、プロトコルのエンコード/デコード等）とする。ウィンドウ表示やデバイス通信そのものは自動テストでは検証しづらいため、後述の手動動作確認で代替する
  - テストフレームワークは **GoogleTest**（v1.15.2、[third_party/googletest](third_party/googletest/NOTICE.md) にvendoring済み）を使う。開発環境にvcpkg/Conan等が無いため、`include`/`src`のみをソースごと同梱しており、各課題の `test/CMakeLists.txt` から相対パスで参照して静的ライブラリとしてビルドする（[01_BuildEnvironmentSetup/test](01_BuildEnvironmentSetup/test) を配線のテンプレートとする）
  - テストを追加する課題では、テスト対象のロジックを `main.cpp`（エントリポイント）から分離して `include/`/`src/` の別ファイルに切り出し、本体の実行ファイルとテスト実行ファイルの両方からリンクできるようにする。テストコードは `NN_AppName/test/` サブフォルダに `test/CMakeLists.txt` として配置し、本体の `CMakeLists.txt` から `add_subdirectory(test)` で取り込む
  - GUI(Win32 API)・ソケット通信・シリアル・USB等、自動テスト化が難しい部分は、ビルド済み `.exe` の実機操作（または外部自動化）による手動動作確認で代替し、確認内容をPR本文の「動作確認」セクションに記載する（過去のPR例: `#67`「マルチクライアントチャットサーバー」のPR本文を参照）

  **Red → Green → Refactor サイクル**

  1. **TODO リスト作成（実装前）** — 実装に入る前に、何をテストするかをリストアップする。コーディング中は視野が狭くなるため、冷静な状態で考える。
  2. **テスト記述** — 1 サイクルで倒すテストは必ず 1 つ。複数テストを同時に通そうとしない。
  3. **Red 確認（必須）** — テストを実行して失敗することを確認する。「期待したエラーメッセージ」であることまで検証してから次に進む。この確認を怠るとバグ原因の特定が困難になる。
  4. **Green（最短経路で合格）** — まず動くコードを書く。一時的な汚さは許容し、リファクタリングは次のフェーズに回す。
  5. **Refactor（外部ふるまいを変えず内部品質を上げる）** — テストが Green の状態を保ちながら実施する。こまめにテストを実行し、Red になったら直前の変更を見直す。時間制限を設けて終わりを決める。
  6. **繰り返し** — TODO リストから完了項目を消し、次のテストに進む。

  **アンチパターン（やってはいけないこと）**
  - 複数テストを同時に倒そうとする
  - Red 確認をスキップして実装に入る
  - Refactor 中にテスト実行を怠る
  - Refactor に時間制限を設けず終わりのないリファクタリングを続ける

- **作業完了時の要件**: 作業を終了する際は、以下の状態で完了すること。これらの要件を満たさずに作業を終了しない
  1. **ビルドエラーがないこと** — 変更した課題フォルダの `cmake --build build`（または `--preset x64-debug`）が成功すること
  2. **警告がないこと** — コンパイラ/IDEの警告が出ていないこと
  3. **テストが全てパスしていること** — GoogleTestを導入済みの課題は当該テストが全て成功すること。未導入の課題（GUI/通信/USB等）はビルド済み `.exe` の手動動作確認結果をPR本文の「動作確認」セクションに記載すること
  4. **コミット・プッシュが完了していること** — ただし**ユーザーの明示的な指示がない限りコミットは実行しない**。コミット可能な状態まで整えたうえでコミットメッセージ案を提示する
